node_modules/
.DS_Store

# Local opencode config
opencode.json

# Throwaway verification scripts
*.tmp.*
verify-*.mjs

# Deno build lockfile (regenerated by `make -C js/lib` / `make -C js/i18n`)
deno.lock
