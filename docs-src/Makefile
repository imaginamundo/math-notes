# Regenerate the documentation site under ../docs/ from the Markdown in src/.
# The generated files are committed and published at /docs.
bundle:
	deno run --node-modules-dir=auto --allow-read --allow-write build.js
