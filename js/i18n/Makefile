# Regenerate the committed Examples content modules from the Markdown sources in
# src/. The runtime lazy-loads examples/<lang>.js.
bundle:
	deno run --node-modules-dir=auto --allow-read --allow-write build.js
