# Regenerate the committed Help/Examples content modules from the Markdown
# sources in src/. The runtime lazy-loads help/<lang>.js and examples/<lang>.js.
bundle:
	deno run --node-modules-dir=auto --allow-read --allow-write build.js
