# Regenerate the bundled mathjs. The runtime serves math.bundle.min.js,
# so also minify the output (e.g. with terser) after bundling.
# The pinned version lives in this file's import and in package.json.
run:
	deno bundle math.js math.bundle.js
