function plus(a, b) { return a + b };
function minus(a, b) { return a - b };
function times(a, b) { return a * b };
function divided(a, b) { return a / b };

export default {
  plus,
  minus,
  times,
  divided
};